NOTE: Some userid was removed while we perform the data filtering like we have removed the dish which has nan values for any of the
      nutrition and hence some user_id was aslo removed. So, while entering the userid it might show error for some userid. So try 
      changing the userid if it shows error and it will work fine.